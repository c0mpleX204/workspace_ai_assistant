# AI 桌面助手开发流程记录

## 1. 下载并部署本地大模型
- 选择合适的开源模型（如Llama-2-7B、Qwen-7B、ChatGLM2-6B等）
- 下载模型权重和依赖环境
- 本地推理测试，确保模型可用

## 2. 微调模型以优化聊天体验(放弃，转为prompt优化)
- 收集或编写个性化对话数据集
- 使用LoRA/QLoRA等轻量微调方法
- 验证微调效果，提升语气自然度

## 3. 后端与前端开发
- 后端：封装模型推理、记忆管理、联网检索、语音处理等API
- 前端：开发桌面UI（如PyQt、Electron），集成聊天、游戏、语音等功能
- 前后端通信接口设计，便于后续模型替换

## 4. 集成语音输入输出
- 语音输入：如SpeechRecognition、Vosk等
- 语音输出：如pyttsx3、Edge TTS等
- UI支持语音交互

## 5. 数据库与记忆能力
- 设计本地数据库（如sqlite、json、向量库）
- 实现对话历史、知识点、用户信息等长期记忆
- 检索与拼接机制，提升多轮对话体验

## 6. 游戏AI训练与集成
- 选择目标游戏，分析画面与操作接口
- 用强化学习等方法训练游戏AI
- 游戏与对话功能解耦，支持独立/协同运行

## 7. 封装与可扩展性
- 关键模块接口化，便于模型/功能替换
- 配置文件管理模型路径、参数、API密钥等
- 文档化各模块用法，方便后续维护与升级

---

> 本文档将持续记录开发过程中的关键步骤、遇到的问题与解决方案。
两周
## 8. 修bug
- 脚本处理chunks的向量时加一个输入document_id，一个个搞
- 修改参数，对话轮数测试的时候太小，我要做全天候agent

flowchart LR
A[麦克风音频] --> B[本地VAD + 语音门限]
B --> C[本地STT<br/>sherpa-sense-voice]
C --> D[意图/状态摘要器<br/>去重+压缩+节流]
D --> E[快回路控制器<br/>规则/状态机]
D --> F[慢回路对话器<br/>LLM]

E --> G[UI即时反馈<br/>字幕/状态灯/动作]
F --> H[回复文本]
H --> I[本地TTS]
I --> J[播放]

F --> K{上游可用?}
K -->|是| L[远程LLM精修]
K -->|否| M[本地小模型兜底]
L --> H
M --> H


常见的 AI Agent 由于需要载入庞大的工具列表和技能系统，天然耗费 Token 且响应缓慢。如果将角色设定与任务指令混杂，不仅容易导致“人设稀释”，还会严重拖累执行效率。

EchoBot 采用 Decision - Roleplay - Agent 三层架构来解决这一痛点：

1. 🧠 Decision Layer (决策层)
负责精准、快速地判断用户意图。

混合意图识别：采用 规则 + 轻量 LLM 双引擎。明确指令直接跳过大模型触发后台任务；模糊意图则交由轻量级 LLM 分类。
智能分发：日常闲聊交由 Roleplay 层；复杂任务则静默唤醒 Agent Core，同时让 Roleplay 层告诉用户已经开始任务。
2. 🎭 Roleplay Layer (角色扮演层)
专注于提供情感价值与沉浸感(๑˃̵ᴗ˂̵) ♡。

纯净上下文：剥离繁杂的 Tool-use 和 Skills 等元数据，专为文本/语音生成优化，确保语气生动、回复极速，不会OOC。
情境感知：根据系统当前状态（如闲聊中、后台任务启动、任务完成等）智能切换专属话术。
3. ⚙️ Agent Core (后台任务层)
在后台静默运行，完成用户指派的任务。

完整 Agent 能力：集成工具链 (Tools)、技能库 (Skills) 以及长短期记忆 (Memory)等能力。
系统级权限：具备操作系统和本地文件读写等高级权限。
异步协同：任务完成后自动整理成果回传，由你的二次元助手亲自向你汇报。