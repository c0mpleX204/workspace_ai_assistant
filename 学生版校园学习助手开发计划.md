# 学生版校园学习助手开发计划与完成度评估

更新日期：2026-05-13

## 1. 当前定位

项目已经从“4周学生项目计划”推进成一个可运行的桌面学习助手原型：

- 桌面端：Electron + Vite + React。
- 后端：FastAPI 服务网关。
- 数据：Postgres 存课程、文档、切片、偏好、学习进度；会话历史同时有本地 JSON 存储。
- 模型：远程 OpenAI-compatible API 为主，区分快模型、重任务模型、视觉模型。
- 核心形态：普通聊天、项目/课程聊天、资料 RAG、陪伴聊天、Live2D、语音输入输出、基础记忆、任务路由。

后续计划不再按“第几天”记录流水账，改成按模块维护完成度和待办。

## 2. 已完成或基本可用

### 桌面与启动

- Electron 桌面壳已可运行。
- 自定义顶栏、工作区侧边栏、普通对话、项目对话、陪伴聊天、设置页已接入。
- 设置页可修改后端地址、用户 ID、模型 API、Live2D 背景、音频设备、TTS 开关。
- 项目对话右上角已有 `CLI` 按钮，可在对应项目根目录打开 PowerShell。

### 普通聊天

- `/chat` 与 `/chat/stream` 已实现。
- 支持会话 ID、多轮历史、压缩摘要、系统人设、错误兜底、延迟显示。
- 支持图片输入，后端可转交视觉模型。
- 支持联网搜索开关。

### 项目/课程资料 RAG

- 支持创建课程/项目，并为每个项目创建本地工作目录。
- 资料上传会保存到对应项目目录。
- 支持解析 `txt/md/pdf/docx/pptx` 和常见代码/文本文件。
- 支持切片、embedding、向量检索、多文档检索。
- 回答可带引用来源、页码/切片信息、源文件路径。
- 后端有资料列表、详情、删除、预览接口。

### 陪伴聊天与多模态

- 陪伴聊天独立页面已接入。
- Live2D 展示与动作/表情意图通道已存在。
- 语音输入支持普通录音 STT，也有 WebSocket + VAD 的实时语音链路。
- TTS 已接入，前端可播放 AI 回复。
- 陪伴聊天有轻重任务路由：轻聊天走快模型，复杂任务可进入后台重任务队列。

### 记忆系统

- 普通聊天有短期窗口、会话摘要、长期偏好、学习进度、提醒注入。
- 陪伴聊天有偏好记忆、会话事实、学习状态、近期提醒、记忆调试接口。
- 已修复陪伴记忆中昵称、语气、喜欢/不喜欢、最近在做什么的中文规则。
- 有基础的重复写入节流和召回筛选。

## 3. 原计划完成度评估

状态说明：

- 已完成：功能已接入主流程，可运行。
- 部分完成：主干存在，但体验、覆盖面或可控性不足。
- 未完成：计划中提到，但代码里没有可用闭环。
- 未验证：代码可能存在，但没有评测或演示结果证明达标。

| 原计划模块 | 当前状态 | 说明 |
| --- | --- | --- |
| 第1周：最小可用聊天助手 | 已完成 | 后端健康检查、聊天接口、远程模型调用、人设、会话历史、基础日志和错误兜底都已具备。 |
| 连续对话 10 轮稳定性验收 | 未验证 | 没有自动化 smoke test 或固定体验记录。 |
| 第2周：学习资料整理与检索 | 部分完成 | 上传、解析、切片、embedding、向量检索、RAG 引用已完成；关键词检索/混合检索和评测集还缺。 |
| 资料管理：列表、删除、按课程过滤 | 部分完成 | 后端接口基本有，前端已有列表/上传/预览，但删除、重命名、课程管理 UI 不完整。 |
| 20 个问题命中率测试 | 未完成 | 没有标准测试集、命中率统计和回归脚本。 |
| 第3周：朋友感与长期记忆 | 部分完成 | 记忆分层、偏好提取、学习进度、召回注入都有；但规则较粗，缺可视化、删除、纠错和污染控制。 |
| 主动提醒策略 | 部分完成 | 后端有 reminder worker 和提醒注入，缺桌面通知/前端提醒中心/用户可编辑提醒。 |
| 学习卡片/错题卡片 | 未完成 | 计划中写了“再说”，目前没有学习卡片生成、复习队列或错题流。 |
| 语气策略稳定性 | 部分完成 | 陪伴人设、意图路由、风格提示已接入；缺系统化体验评测。 |
| 第4周：多模态模型路由 | 部分完成 | 文本、视觉、语音服务都有，但还不是统一的多模态接口层。 |
| 图像输入 | 已完成 | 前端可上传图片，后端会走视觉模型。 |
| STT/TTS | 已完成 | 后端有 `/stt`、`/stt/ws`、`/tts`，前端普通聊天和陪伴聊天都已接入部分能力。 |
| 成本统计 | 未完成 | 目前主要记录 latency，没有请求次数、token、费用估算、日预算或降级策略。 |
| 配置文档和快速启动脚本 | 部分完成 | `指令.txt` 已整理为当前可用命令；仍缺一键启动脚本和完整 README。 |
| 前端 UI 优化 | 部分完成 | 主界面已比较完整；仍缺文件浏览器、资料删除、设置项细化、移动/窄屏细节打磨。 |
| 设置页动态配置 | 部分完成 | 模型 API、服务地址、音频、Live2D 背景可配；记忆参数、风格、人设、路由策略还不完整。 |
| 联网 RAG | 已完成 | 已有 DuckDuckGo 搜索服务和前端联网开关；缺来源质量评估和失败提示细化。 |
| 演示材料、视频、简历包装 | 部分完成 | 旧计划里有简历模板，但没有正式演示脚本、截图、视频和最终项目 README。 |
| 第5周：类人长时记忆机制 | 部分完成 | 有摘要、偏好、学习进度、提醒和基础召回；还没有历史对话 embedding 记忆库。 |
| 历史对话 embedding 存储与向量检索 | 未完成 | 当前 embedding 主要用于资料切片；长期对话回忆还不是向量库方案。 |
| 回忆触发机制 | 部分完成 | 陪伴聊天有关键词/重叠度召回，普通聊天有相关记忆筛选；没有实体识别、高阈值控制和跨话题评测。 |
| 记忆摘要与遗忘机制 | 部分完成 | 摘要已做；遗忘、归档、用户确认、手动删除和污染修复还缺。 |
| 记忆机制设计文档 | 未完成 | 有零散设计文档，但缺一份专门说明记忆层、写入规则、召回规则和安全边界的文档。 |
| 意图检测与风格切换 | 部分完成 | 有规则 + 轻量 LLM 路由，能区分闲聊、知识、动作、重任务；没有 BERT/fasttext 等独立分类器。 |
| 主动回忆与高效检索 | 部分完成 | 有每轮召回，但还不是本地向量库、异步预检索、主题聚类、高频事件标签。 |

## 4. 当前最重要的未完成项

### P0：先补闭环

1. 自动化冒烟测试  
   覆盖 `/health`、普通 `/chat`、项目 RAG、资料上传、STT/TTS 可用性。至少能一键判断今天有没有跑坏。

2. RAG 评测集  
   建 20 个固定问题，记录命中来源、回答正确性、引用是否有效。原计划里的 70% 命中率需要真正量化。

3. 资料/项目管理 UI  
   前端补删除资料、重命名项目、删除项目、刷新索引、查看源文件路径。现在后端能力比前端入口多。

4. 记忆可视化与清理  
   做一个“记忆管理”页，能查看、删除、修正用户偏好、学习进度、提醒和会话事实。

5. 成本与调用统计  
   记录每次模型调用的模型名、耗时、成功/失败、估算 token、估算费用；设置日预算和降级策略。

### P1：补学习助手核心价值

1. 学习卡片与复习队列  
   从资料或对话中生成知识点卡片、错题卡片、复习计划，并写入 learning_progress。

2. 主动提醒前端闭环  
   后端已有提醒 worker，但需要桌面通知、提醒中心、稍后提醒、标记完成。

3. 历史对话向量记忆  
   将重要历史事件做 embedding，按用户、会话、主题检索，避免只靠规则和摘要。

4. 项目文件浏览器  
   显示项目目录里的已索引/未索引文件，支持打开、预览、按需解析。

5. 代码项目工具  
   对代码文件树、文件内容、模块摘要、运行命令做受控工具化，而不是只靠上传资料 RAG。

### P2：体验与展示

1. 演示脚本和 README  
   写清楚 3 条演示路径：普通聊天、资料问答、陪伴语音/Live2D。

2. 体验评测记录  
   做一次 2 小时真实使用走查，记录卡顿、误触发、记忆污染、语音误识别。

3. 意图分类增强  
   目前规则 + LLM 足够原型，但后续可加入轻量本地分类器或缓存路由结果。

4. 主动回忆优化  
   增加实体识别、主题标签、相似度阈值和异步预检索，避免无关记忆插入。

## 5. 建议下一阶段节奏

### 第一个小冲刺：把项目变得“可验收”

- 写 smoke test 脚本。
- 做 20 题 RAG 评测集。
- 整理 README 和演示脚本。
- 修正最影响演示的 UI 缺口。

### 第二个小冲刺：把记忆变得“可控”

- 做记忆管理页。
- 给记忆写入加确认/撤销机制。
- 增加历史对话向量召回。
- 写一份记忆机制设计文档。

### 第三个小冲刺：把学习功能做实

- 生成知识点卡片。
- 生成错题/薄弱点卡片。
- 接入复习计划和桌面提醒。
- 用评测集验证资料问答质量。

## 6. 简历表述草稿

项目名：校园学习伙伴（桌面端多模态学习助手）

- 基于 FastAPI + Electron + React 构建桌面学习助手，支持普通聊天、项目资料问答、陪伴聊天和语音交互。
- 实现课程资料上传、解析、切片、embedding 检索与带引用回答，支持 PDF、DOCX、PPTX、Markdown 与常见代码文件。
- 设计短期会话、压缩摘要、长期偏好、学习进度与提醒注入的分层记忆机制，提升多轮对话连续性。
- 接入 Live2D、STT、TTS、视觉模型与轻重任务路由，为后续多模态和后台 Agent 能力扩展打底。

## 7. 保留风险

- 没有自动化测试时，功能多了以后容易回归。
- 记忆没有可视化和删除入口时，容易污染用户体验。
- RAG 没有评测集时，无法证明“引用回答”真的准确。
- 语音和 Live2D 体验受本地设备、模型、延迟影响较大，需要单独调参。
- 成本统计缺失时，不适合长时间开放使用。
